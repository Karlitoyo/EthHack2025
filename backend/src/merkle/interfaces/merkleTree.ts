export type PatientRow = {
    hospital_id: string;
    treatment: string;
    patient_id: string;
  };

  export interface PublicPatientProofRow {
    hospital_id: string;
    treatment: string;
    patient_id: string;
  }