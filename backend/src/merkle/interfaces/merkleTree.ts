export type PatientRow = {
    country_id: string;
    relation: string;
    citizen_id: string;
  };

  export interface PublicPatientProofRow {
    country_id: string;
    relation: string;
    citizen_id: string;
  }